Contract SecondaryRecord(
  registrar: ByteVec,
  mut owner: Address,
  mut resolver: Resolver,
  mut ttl: U256,
  refundAddress: Address
) extends Constants(), RecordBase(registrar, owner, resolver, ttl, refundAddress) {
}