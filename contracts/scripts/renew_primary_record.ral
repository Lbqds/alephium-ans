TxScript RenewPrimaryRecord(registrar: PrimaryRegistrar, name: ByteVec, credentialTokenId: ByteVec, duration: U256) {
  let caller = callerAddress!()
  registrar.renew{caller -> ALPH: tokenRemaining!(caller, ALPH), credentialTokenId: 1}(name, caller, duration)
}