TxScript Register(registrarId: ByteVec, name: ByteVec, rentalPeriod: U256) {
  let registrar = Registrar(registrarId)
  let owner = callerAddress!()
  registrar.register{owner -> ALPH: tokenRemaining!(owner, ALPH)}(name, owner, rentalPeriod, owner)
}
