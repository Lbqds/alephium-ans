TxScript Register(registrar: Registrar, name: ByteVec, rentalPeriod: U256, resolver: Resolver) {
  let owner = callerAddress!()
  registrar.register{owner -> ALPH: tokenRemaining!(owner, ALPH)}(name, owner, rentalPeriod, owner, resolver)
}
